import { PlaceholderPage } from "../_shell/PlaceholderPage";

export default function OperationsPage() {
  return (
    <PlaceholderPage
      title="Operations"
      description="The lead-to-cash pipeline hub — Quotes and Applications. Live feed lands in a later phase — see docs/portal/v2-design.md §13."
    />
  );
}
