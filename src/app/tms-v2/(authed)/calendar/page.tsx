import { PlaceholderPage } from "../_shell/PlaceholderPage";

export default function CalendarPage() {
  return (
    <PlaceholderPage
      title="Calendar"
      description="Month-at-a-glance load spans + maintenance dates. Live, windowed query lands in a later phase — see docs/portal/v2-design.md §8."
    />
  );
}
