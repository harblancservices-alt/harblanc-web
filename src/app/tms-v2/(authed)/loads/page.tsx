import { PlaceholderPage } from "../_shell/PlaceholderPage";

export default function LoadsPage() {
  return (
    <PlaceholderPage
      title="Load Board"
      description="Every load ever booked, server-paginated and searchable. Live data lands in a later phase — see docs/portal/v2-design.md §4."
    />
  );
}
