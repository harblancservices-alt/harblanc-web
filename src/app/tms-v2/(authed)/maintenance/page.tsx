import { PlaceholderPage } from "../_shell/PlaceholderPage";

export default function MaintenancePage() {
  return (
    <PlaceholderPage
      title="Maintenance"
      description="Odometer-driven service and repair records. Live data lands in a later phase — see docs/portal/v2-design.md §24."
    />
  );
}
