import { PlaceholderPage } from "../_shell/PlaceholderPage";

export default function BrokersPage() {
  return (
    <PlaceholderPage
      title="Brokers"
      description="Master directory of every broker partner. Live data lands in a later phase — see docs/portal/v2-design.md §9."
    />
  );
}
