import { PlaceholderPage } from "../_shell/PlaceholderPage";

export default function AccountingPage() {
  return (
    <PlaceholderPage
      title="Accounting"
      description="Real aggregate MTD-collected figures, linked from Receivables rather than re-derived. Live data lands in a later phase — see docs/portal/v2-design.md §22."
    />
  );
}
