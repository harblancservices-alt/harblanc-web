import type { Metadata } from "next";
import HomePreviewPage from "@/components/previews/HomePreview";

export const metadata: Metadata = {
  title: "Home — preview",
  robots: { index: false, follow: false },
};

export default HomePreviewPage;
