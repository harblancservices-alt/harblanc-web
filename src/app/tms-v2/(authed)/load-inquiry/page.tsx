import { PlaceholderPage } from "../_shell/PlaceholderPage";

export default function LoadInquiryPage() {
  return (
    <PlaceholderPage
      title="Load Inquiry"
      description="One-off, fast broker inquiry email tool. Live flow lands in a later phase — see docs/portal/v2-design.md §11."
    />
  );
}
