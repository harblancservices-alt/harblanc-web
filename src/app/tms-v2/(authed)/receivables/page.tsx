import { PlaceholderPage } from "../_shell/PlaceholderPage";

export default function ReceivablesPage() {
  return (
    <PlaceholderPage
      title="Receivables"
      description="Carrier-load A/R and customer-quote A/R as two labeled rows of one page. Live data lands in a later phase — see docs/portal/v2-design.md §20."
    />
  );
}
