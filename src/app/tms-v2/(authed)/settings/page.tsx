import { PlaceholderPage } from "../_shell/PlaceholderPage";

export default function TmsV2SettingsPage() {
  return (
    <PlaceholderPage
      title="Settings"
      description="Company identity, display preferences, and account controls. Live form lands in a later phase — see docs/portal/v2-design.md §27."
    />
  );
}
