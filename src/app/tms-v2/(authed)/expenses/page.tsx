import { PlaceholderPage } from "../_shell/PlaceholderPage";

export default function ExpensesPage() {
  return (
    <PlaceholderPage
      title="Expenses"
      description="Load and trip expense tracking with server-persisted saved filters. Live data lands in a later phase — see docs/portal/v2-design.md §21."
    />
  );
}
