import { PlaceholderPage } from "../_shell/PlaceholderPage";

export default function PerformancePage() {
  return (
    <PlaceholderPage
      title="Performance"
      description="Server-aggregated period rollups through the one money engine. Live data lands in a later phase — see docs/portal/v2-design.md §23."
    />
  );
}
