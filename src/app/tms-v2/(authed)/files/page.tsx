import { PlaceholderPage } from "../_shell/PlaceholderPage";

export default function FilesPage() {
  return (
    <PlaceholderPage
      title="Files"
      description="Every document across loads, quotes, and BOLs, server-paginated. Live data lands in a later phase — see docs/portal/v2-design.md §25."
    />
  );
}
