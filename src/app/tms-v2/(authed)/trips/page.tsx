import { PlaceholderPage } from "../_shell/PlaceholderPage";

export default function TripsPage() {
  return (
    <PlaceholderPage
      title="Trips"
      description="Out-and-back run grouping with trip-level P&L. Live data lands in a later phase — see docs/portal/v2-design.md §6."
    />
  );
}
