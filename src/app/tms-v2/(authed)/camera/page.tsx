import { PlaceholderPage } from "../_shell/PlaceholderPage";

export default function CameraPage() {
  return (
    <PlaceholderPage
      title="Camera"
      description="Batch photo capture and review. Live data lands in a later phase — see docs/portal/v2-design.md §26."
    />
  );
}
