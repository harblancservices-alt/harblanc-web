import { PlaceholderPage } from "../_shell/PlaceholderPage";

export default function ReachPage() {
  return (
    <PlaceholderPage
      title="Reach"
      description="Near-zero-typing bulk backhaul outreach. Live flow lands in a later phase — see docs/portal/v2-design.md §12."
    />
  );
}
